# Known divergences of `succinctly yq` from the pinned-yq golden fixtures.
#
# Consumed by tests/yq_golden_tests.rs. One line per failing case:
#
#   <case>            <category>        <reason — #issue>
#
# <case> is a directory name under tests/data/yq-golden/cases/. Columns are
# whitespace-separated; `#` lines and blank lines are ignored.
#
# The check is two-sided: a case failing without a line here fails the build,
# and a line here for a case that now passes also fails the build — so every
# fix is forced to shrink this file, and no divergence can hide. The goldens
# themselves always hold the correct (pinned yq) output; only this manifest
# records where succinctly currently disagrees. See
# tests/data/yaml-test-suite-known-failures.txt for the sibling manifest.
#
# Seeded by #573 with the first case whose golden carries expected.status/err
# instead of a passing expected.out: yq rejects the input outright, and
# succinctly instead exits 0 with corrupted output.

seq_item_double_outdent_rejected  yaml_parser  a block sequence continuation that out-dents past its immediate enclosing frame is accepted with corrupted output instead of rejected like yq — #519

#
# Extended by #2416 phase 0 (the oracle-backed net for retiring the eager
# path-context evaluator). Every entry was captured live from yq v4.53.3 by
# scripts/jq-path-context-oracle-sweep.sh and is recorded rather than deleted:
# the point of a net is that a shape it caught stays caught. The sweep's own
# class-level manifest is tests/data/jq-path-context-sweep-known-divergences.txt.
#
# The two `root_*` entries are the first cases in this corpus whose golden is a
# *silent success* — yq exits 0 having printed nothing — which is why they
# carry an expected.empty marker (see tests/yq_golden_tests.rs).

path_ctx_root_key           root_edge      `key` at the document root prints `null`; real yq emits nothing at all — #2421
path_ctx_root_parent        root_edge      `parent` at the document root prints `{}`; real yq emits nothing at all — #2421
path_ctx_parent_above_root  root_edge      `[.a | parent | parent]` is `[{}]`; real yq stops at the root and yields `[]` — #2421
path_ctx_object_key         object_arm     `[.a[] | {"k": key}]` is `[{"k":null},{"k":null}]`; the path-context evaluator has no Expr::Object arm, so the leaf loses its path — #1332
path_ctx_recurse_path       descend_path   `[.. | path]` is all `[]`; recursive descent never threads the accumulated path into its outputs — #2416
path_ctx_recurse_key        descend_path   `[.. | key]` is all `null`, same cause as path_ctx_recurse_path — #2416
path_ctx_recurse_kind_path  descend_path   `[.. | select(kind == "scalar") | path]` is all `[]`, same cause as path_ctx_recurse_path — #2416
path_ctx_path_with_arg      yq_gating      `path(.a)` answers `["a"]` and exits 0; real yq has no `path(f)` at all (only bare `path`) and rejects it with "bad expression, please check expression syntax" — #1512
